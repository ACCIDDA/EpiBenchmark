# API Reference

This section is a placeholder API index for the EpiBench source modules.

Each page in this section is backed by `mkdocstrings`, so the final rendered
content will come from the Python source code and its docstrings.

Use this page to add:

- A short explanation of the package layout
- Guidance on which modules are public entry points
- Notes about stability or intended external use
